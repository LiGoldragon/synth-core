;; tree.aski — the data-tree
;;
;; The data-tree is the output of askic — a populated Node tree
;; representing a compiled .aski program.
;;
;; DialectKind, Rule, and grammar structures belong in askicc
;; (compiler infrastructure), not in aski-core (language anatomy).

(Tree DataTree)

{DataTree
  (Root Node)
  Scope}
